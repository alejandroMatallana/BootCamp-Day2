import { MyColorStyleDirective } from './my-color-style.directive';

describe('MyColorStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new MyColorStyleDirective();
    expect(directive).toBeTruthy();
  });
});
