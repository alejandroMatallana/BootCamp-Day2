import { PaiPruebaPipe } from './pai-prueba.pipe';

describe('PaiPruebaPipe', () => {
  it('create an instance', () => {
    const pipe = new PaiPruebaPipe();
    expect(pipe).toBeTruthy();
  });
});
